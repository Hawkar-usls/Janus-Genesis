{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Hawkar-usls/Janus_Genesis/schemas/janus_113_8_router_ledger_entry_v2.schema.json",
  "title": "JANUS 113.8 Hardened Router Witness Ledger Entry v2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "ordinal",
    "input_line_sha256",
    "prediction_sha256",
    "prev_hash",
    "entry_hash"
  ],
  "properties": {
    "schema": {
      "const": "janus.genesis.router.ledger_entry.v2"
    },
    "ordinal": {
      "type": "integer",
      "minimum": 0
    },
    "input_line_sha256": {
      "$ref": "#/$defs/sha256"
    },
    "prediction_sha256": {
      "$ref": "#/$defs/sha256"
    },
    "prev_hash": {
      "$ref": "#/$defs/sha256"
    },
    "entry_hash": {
      "$ref": "#/$defs/sha256"
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    }
  },
  "x-janus-ledger-rule": {
    "genesis_prev_hash": "64 lowercase zero characters",
    "entry_hash": "SHA256('JANUS_ROUTER_LEDGER_V2\\n' || canonical JSON of schema, ordinal, input_line_sha256, prediction_sha256, prev_hash)",
    "conservation": "Exactly one ledger entry for every non-empty input line, including malformed, duplicate-key, collision, rejected, resource-limited, OPEN, and safety-blocked lines."
  }
}
